Fibo122 error with ) ?!?!

space concatenation (C header issue)

xss embedding deeley

grades grid

view history by student or problem (or delete students)

links, playlists

in list.php: folder expansion, blurbs, tags

parsons problems? should be doable using on("changes") and
"origin" object of each change (one "drag", one "paste", with
complementary text/removed)

check API (e.g. CombinationLock check private inst vars)

add valgrind for c++?

randomization!

graphics!
